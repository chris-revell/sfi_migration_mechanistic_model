Useful bash commands/scripts

Bash command for multiple runs:
for i in {1..10}; do for j in {1..10}; do python3 goose_sim.py NDVI_data/spring $(($i*100000)) $(($j*250)); done; done

imagemagick
for i in $(ls); do montage -title "$i" -geometry 600x $i/distance.png $i/path.png combined_plots/$i.png; done

To plot all data in a set of folders
for i in $(ls); do cd $i; gnuplot -c ../../gnuplot_commands.gnu; cd ..; done
